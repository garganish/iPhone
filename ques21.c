#include <math.h>#include <stdio.h>#include <string.h>#include <stdlib.h>#include <assert.h>#include <limits.h>#include <stdbool.h>int main() {char str[100]="",pat1[20]="",pat2[20]=""; int loc1,loc2;gets(str);gets(pat1);gets(pat2);scanf("%d",&loc1);scanf("%d",&loc2);/* Enter your code here. Read input from STDIN. Print output to STDOUT */ char newString[250] = {'\0'};strncpy(newString, str, loc1);strcat(newString, pat1);strncat(newString, str+loc1,loc2-loc1-strlen(pat1));strcat(newString, pat2);strcat(newString, str+loc2-strlen(pat1)); puts(newString);return 0; }